# Aemanbo Frontend

Vue 3 + Vite frontend for the Aemanbo project.

## Setup

```sh
npm install
```

## Development

```sh
npm run dev
```

The dev server runs on `http://localhost:5173`.

API requests under `/api` are proxied to `http://127.0.0.1:8000`.

## Build

```sh
npm run build
```
